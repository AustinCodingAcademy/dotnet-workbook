@*allows us to use the types in the SportsStore.Models namespace in views without needing to refer to the namespace*@
@using SportsStore.Models

@*allows built-in tag helpers, which we can later use to create HTML elements that reflect the configuration of the application.*@
@addTagHelper*, Microsoft.AspNetCore.Mvc.TagHelpers